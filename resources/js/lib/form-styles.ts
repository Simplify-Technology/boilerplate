/**
 * Tipografia para linhas `Label` + controle avulsas (a mesma dos labels do FormField).
 */
export const FORM_CONTROL_LABEL_CLASSNAME = 'text-foreground flex min-w-0 items-center gap-1.5 text-sm leading-none font-medium';
