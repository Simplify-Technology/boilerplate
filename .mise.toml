# Toolchain do projeto (mise). Instale com: mise install
[tools]
node = "24"      # Node 24 LTS — mesma major do CI (matrix node: [24])
pnpm = "11.19.0" # mesma versão do packageManager no package.json
